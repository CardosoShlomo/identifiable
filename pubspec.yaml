name: identifiable
description: Identifiable mixin and id-based collection helpers for Dart.
version: 0.5.0
repository: https://github.com/CardosoShlomo/identifiable

environment:
  sdk: ^3.12.0

dependencies:
  canon_codec: ^0.1.4
  meta: ^1.16.0

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
